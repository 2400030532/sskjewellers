import { STORE_CONFIG } from '../../products.js';
import { whatsappUrl } from '../bootstrap.js';

export default function CustomDesign() {
  return <section className="page custom-page shell" id="page-content"><div className="custom-page-intro"><span className="eyebrow">Want something made?</span><h1>Tell us what you want</h1><p>Share a photo or describe your idea. We can make jewellery to suit your style, weight, and budget.</p></div><form className="request-form" onSubmit={event => { event.preventDefault(); const data = new FormData(event.currentTarget); const message = `Namaste Sri Sai Krishna Jewellers, I would like help with a custom design. Name: ${data.get('name')}. Jewellery: ${data.get('type')}. Approximate weight: ${data.get('weight')}g. Details: ${data.get('details') || 'I will share more on WhatsApp.'}`; window.open(whatsappUrl(message), '_blank', 'noopener,noreferrer'); }}><label>Your name<input name="name" required placeholder="Your name" /></label><label>What would you like?<select name="type"><option>Gold jewellery</option><option>Silver jewellery</option><option>Wedding jewellery</option><option>Something else</option></select></label><label>Approximate weight<input name="weight" type="number" min="1" step="0.5" required placeholder="For example, 20" /></label><label>Tell us more<textarea name="details" rows="5" placeholder="Share your idea, budget, or a design photo on WhatsApp."></textarea></label><button className="whatsapp-button" type="submit">Send request on WhatsApp</button></form><p className="form-note">You can attach a design photo in the WhatsApp chat that opens.</p></section>;
}
